# Benchmarks

This directory will contain performance benchmarks for the `sdmx-parsers` deserialisation engine, built using Criterion.

## Planned Benchmarks

- `parsing_benchmark.rs` — Benchmarks XML, JSON, and CSV parsing performance for large metadata and data messages.
