# Automatically load the Nix development environment defined in flake.nix
use flake

# Ensure GPG signing prompts render in the active terminal window
export GPG_TTY=$(tty 2>/dev/null || true)

# Load maintainer-local overrides if present (untracked; not for contributors)
source_env_if_exists .envrc.local
